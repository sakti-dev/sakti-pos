{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main"],
  "permissions": [
    "android-fs:default",
    "core:default",
    "log:default",
    "opener:default"
  ]
}
