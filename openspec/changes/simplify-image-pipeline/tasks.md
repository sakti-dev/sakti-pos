## 1. Plugin API Simplification — `get_asset_path`

- [x] 1.1 Write test: `get_asset_path` returns compressed path when file exists
- [x] 1.2 Write test: `get_asset_path` falls back to preview when no compressed file
- [x] 1.3 Write test: `get_asset_path` returns null when neither file exists
- [x] 1.4 Implement `get_asset_path` command in `tauri-plugin-image-pipeline/src/commands.rs` — checks compressed file first, then preview, then null
- [x] 1.5 Register `get_asset_path` in `lib.rs` invoke handler and `build.rs` COMMANDS
- [x] 1.6 Add permission entry in `permissions/default.toml` and capabilities
## 2. Plugin Cache Layout — Remove merchant_id

- [x] 2.1 Write test: `asset_cache_path(cache_root, asset_id, content_type)` produces flat path `<cache_root>/assets/<assetId>.webp`
- [x] 2.2 Write test: `preview_cache_path(cache_root, job_id)` produces `<cache_root>/previews/<jobId_prefix>_preview.jpg`
- [x] 2.3 Update `cache.rs` functions to remove `merchant_id` parameter and produce flat paths
- [x] 2.4 Update `pick_image` to use flat cache layout (no merchant subdirectories)
- [x] 2.5 Update `compress_asset` to use flat cache layout
- [x] 2.6 Update Android backend to use flat cache layout
- [x] 2.7 Run all plugin tests, fix any broken path assertions
## 3. Plugin Command Cleanup — Remove Unused Commands

- [x] 3.1 Remove `enqueue_job`, `process_pending_jobs`, `get_completed_jobs`, `consume_completed_job` from commands.rs, lib.rs, build.rs
- [x] 3.2 Remove `reset_stuck_jobs`, `retry_failed_job`, `get_failed_jobs` from commands.rs, lib.rs, build.rs
- [x] 3.3 Remove `get_pending_preview`, `get_cached_asset_path` (replaced by `get_asset_path`) from commands.rs, lib.rs, build.rs
- [x] 3.4 Remove `cleanup_orphaned_temp_files` from commands.rs, lib.rs, build.rs
- [x] 3.5 Clean up permissions: remove autogenerated TOML files for deleted commands, update `default.toml`
- [x] 3.6 Delete dead code: `queue_state.rs`, `job_queue.rs`, `processor.rs` (job queue internals)
- [x] 3.7 Update `dto.rs` — remove DTOs for deleted commands, keep only `PickImage*`, `CompressAsset*`, `DeleteAsset*`, `CachedPathResponse`
- [x] 3.8 Run `cargo check` and all plugin tests — verify clean build with 3 commands only
## 4. Plugin Guest-JS Bridge

- [x] 4.1 Update `guest-js/index.ts` — export `pickImage`, `compressAsset`, `getAssetPath`, `deleteAsset` only
- [x] 4.2 Remove old guest-js exports for deleted commands
## 5. JS Asset Upload Module

- [x] 5.1 Write test: `uploadSingleAsset` calls presign → S3 PUT → complete-upload → writeTransaction on success
- [x] 5.2 Write test: `uploadSingleAsset` leaves asset as `compressed` when presign fails
- [x] 5.3 Write test: `uploadSingleAsset` leaves asset as `compressed` when S3 PUT fails
- [x] 5.4 Write test: `uploadPendingAssets` queries compressed assets and uploads each
- [x] 5.5 Write test: `uploadPendingAssets` returns 0 when no compressed assets
- [x] 5.6 Implement `apps/pos-app/src/lib/assets/upload.ts` — `uploadSingleAsset`, `uploadPendingAssets`
- [x] 5.7 Read compressed file bytes from plugin cache via `invoke("get_asset_path")` + `fs.readBinaryFile`
## 6. JS Asset Lifecycle Module

- [x] 6.1 Write test: lifecycle listener receives `asset://compressed` event → writeTransaction updates asset to `compressed` with metadata
- [x] 6.2 Write test: lifecycle listener triggers upload after updating asset
- [x] 6.3 Write test: lifecycle listener logs warning when no matching asset row
- [x] 6.4 Implement `apps/pos-app/src/lib/assets/lifecycle.ts` — `startAssetLifecycleListener`
- [x] 6.5 Wire lifecycle listener in `apps/pos-app/src/lib/app/listeners.ts`
## 7. JS Asset Recovery Module

- [x] 7.1 Write test: `recoverAssets` queries pending assets and invokes `compress_asset` for each
- [x] 7.2 Write test: `recoverAssets` queries compressed assets and triggers upload for each
- [x] 7.3 Write test: `recoverAssets` does nothing when no stuck assets
- [x] 7.4 Implement `apps/pos-app/src/lib/assets/recovery.ts` — `recoverAssets`
## 8. JS Asset URL Resolution — Simplify `cache.ts`

- [x] 8.1 Write test: `resolveAssetUrl(assetId)` invokes `get_asset_path` and returns `convertFileSrc` URL
- [x] 8.2 Write test: `resolveAssetUrl(assetId)` returns null when plugin returns null
- [x] 8.3 Write test: `resolveAssetUrl(null)` returns null without invoking plugin
- [x] 8.4 Simplify `apps/pos-app/src/lib/assets/cache.ts` — keep only `resolveAssetUrl`, remove version stores, `notifyAssetCacheReady`, `notifyAssetAttachmentReady`, `getAssetCacheVersion`, `getDomainCatalogVersion`, all reset functions
- [x] 8.5 Update `apps/pos-app/src/components/image.tsx` — replace adapter `useImageUrl` with direct `resolveAssetUrl` call
- [x] 8.6 Update `apps/pos-app/src/pages/pos/use-pos.ts` — remove `getDomainCatalogVersion` import, remove cache version from query key
- [x] 8.7 Update `apps/pos-app/src/pages/settings/product-categories/product-list.tsx` — remove `getDomainCatalogVersion` import
## 9. Delete Reactive Cache Layer

- [x] 9.1 Delete `apps/pos-app/src/lib/assets/create-adapter.ts`
- [x] 9.2 Delete `apps/pos-app/src/lib/assets/adapters/` directory
- [x] 9.3 Delete `apps/pos-app/src/lib/assets/types.ts`
- [x] 9.4 Update `apps/pos-app/src/lib/app/listeners.ts` — remove `productImageAdapter.startEventListeners()` call, start lifecycle listener instead
- [x] 9.5 Update all imports — remove references to `createAssetAdapter`, `productImageAdapter`, `AssetAdapter`, `AssetAdapterConfig`
- [x] 9.6 Delete or update tests in `__test__/create-adapter.test.ts`, `__test__/cache.test.ts`
## 10. Rewrite `sync.ts` from Rust Bridge to JS Orchestrator

- [x] 10.1 Write test: `uploadPendingProductImages` calls `uploadPendingAssets` from `upload.ts`
- [x] 10.2 Rewrite `apps/pos-app/src/lib/assets/sync.ts` — `uploadPendingAssets` calls JS upload module, `hydrateMissingAssets` returns stub
- [x] 10.3 Update `apps/pos-app/src/store/sync.ts` — remove `startAssetCompressedListener` (moved to lifecycle.ts), keep sync order: syncNow → upload → hydrate
- [x] 10.4 Update `apps/pos-app/src/store/sync.ts` — call `recoverAssets()` after startup sync
## 11. Update Product Form

- [x] 11.1 Write test: product form creates assets row via `writeTransaction` with status='pending' on submit
- [x] 11.2 Write test: product form invokes `compress_asset` fire-and-forget after creating asset
- [x] 11.3 Write test: product edit mode calls `delete_asset` for old asset via plugin
- [x] 11.4 Update `apps/pos-app/src/pages/settings/product-categories/product-form.tsx` — use `writeTransaction` for asset row creation, fire-and-forget `compress_asset`
- [x] 11.5 Remove direct `invoke("get_cached_asset_path")` calls from product-form — use `get_asset_path` via plugin-bridge instead
## 12. Delete Rust `assets/` Module

- [x] 12.1 Remove `mod assets` from `apps/pos-app/src-tauri/src/lib.rs`
- [x] 12.2 Delete entire directory: `apps/pos-app/src-tauri/src/assets/`
- [x] 12.3 Remove `reqwest` dependency from `apps/pos-app/src-tauri/Cargo.toml` (if only used by upload_queue.rs)
- [x] 12.4 Remove asset-related Tauri command registrations from `apps/pos-app/src-tauri/src/lib.rs`
- [x] 12.5 Remove recovery/listener setup from `apps/pos-app/src-tauri/src/app/startup.rs`
## 13. Plugin Bridge Update

- [x] 13.1 Update `apps/pos-app/src/lib/assets/plugin-bridge.ts` — replace `invoke("plugin:image-pipeline|get_cached_asset_path")` with `invoke("plugin:image-pipeline|get_asset_path")`
- [x] 13.2 Remove `merchantId` from all plugin-bridge invoke calls
- [x] 13.3 Update `pluginDeleteAsset` to use updated `delete_asset` command signature
## 14. Integration Verification

- [x] 14.1 Run `cargo check` for both plugin and app — verify clean build
- [x] 14.2 Run `bun x ultracite check` — verify no lint errors
- [x] 14.3 Run plugin tests: `cargo test --manifest-path tauri-plugin-image-pipeline/Cargo.toml`
- [x] 14.4 Run JS tests: `bun test apps/pos-app/src/lib/assets/__test__/`
- [x] 14.5 Update `logs/capture-adb-logcat.sh` LOG_FILTER for new log prefixes
- [x] 14.6 Update `docs/DOCUMENTED-LOG-PREFIX.md` with new lifecycle/upload log prefixes
- [x] 14.7 Manual E2E test on Android: pick → preview → submit → compress → upload → ready
