## 1. Vendor tauri-plugin-dialog

- [x] 1.1 Inside `vendor/tauri-plugin-image-pipeline/`, add `tauri-plugin-dialog` as a git subtree at `vendor/tauri-plugin-dialog/` from `https://github.com/tauri-apps/tauri-plugin-dialog.git` (pin to a stable tag, e.g. `v2.7.1` or latest `v2` release)
- [x] 1.2 Update `vendor/tauri-plugin-image-pipeline/Cargo.toml` to change `tauri-plugin-dialog = "2"` to `tauri-plugin-dialog = { path = "vendor/tauri-plugin-dialog" }`
- [x] 1.3 Verify `cargo check` resolves the dialog dependency from the local subtree path
- [x] 1.4 Verify the POS app's `tauri_plugin_dialog::init()` still works — vendoring changes the Cargo source path but Tauri still requires explicit plugin registration in `apps/pos-app/src-tauri/src/lib.rs`

## 2. Delete Obsolete Modules

- [x] 2.1 Delete `vendor/tauri-plugin-image-pipeline/src/job_queue.rs`
- [x] 2.2 Delete `vendor/tauri-plugin-image-pipeline/src/pipeline.rs`
- [x] 2.3 Delete `vendor/tauri-plugin-image-pipeline/src/queue_state.rs`
- [x] 2.4 Delete `vendor/tauri-plugin-image-pipeline/tests/job_queue.rs`
- [x] 2.5 Delete `vendor/tauri-plugin-image-pipeline/tests/queue_state.rs`
- [x] 2.6 Delete `vendor/tauri-plugin-image-pipeline/tests/scaffold_api.rs`

## 3. Simplify DTOs

- [x] 3.1 Rewrite `dto.rs` — keep only: `PickImageRequest`, `PickImageResponse`, `PickImageCompressionOptions`, `CompressAssetRequest`, `CompressAssetResponse`, `DeleteAssetRequest`, `CachedPathResponse`, `JobCompletedPayload`
- [x] 3.2 Remove all queue-related DTOs: `EnqueueJobRequest`, `ProcessJobsResponse`, `CompletedJob`, `FailedJob`, `JobResult`, `JobRecord`, `JobStatus`, `QueueDocument`, `AttachmentLookup`, `PreviewPathResponse`, and any other DTOs only used by deleted commands
- [x] 3.3 Remove `merchant_id`, `entity_type`, `entity_id`, `attachment_field`, `processing_kind` from all remaining DTOs
- [x] 3.4 Add `assetId` field to `CompressAssetRequest`
- [x] 3.5 `CachedPathResponse` contains `{ localPath }` only — matches JS `cache.ts` which reads only `result.localPath`

## 4. Simplify Cache Module

- [x] 4.1 Rewrite `cache.rs` — remove `merchant_id` parameter from all functions
- [x] 4.2 Implement flat `asset_cache_path(cache_root, asset_id)` → `<cache_root>/assets/<assetId>.webp`
- [x] 4.3 Implement flat `preview_cache_path(cache_root, job_id)` → `<cache_root>/previews/<jobId_prefix>_preview.jpg`
- [x] 4.4 Implement flat `staged_source_path(cache_root, job_id)` → `<cache_root>/picked/<jobId>.source`
- [x] 4.5 Remove all merchant-scoped path construction logic

## 5. Simplify Commands

- [x] 5.1 Rewrite `commands.rs` — keep only 4 command handlers: `pick_image`, `compress_asset`, `get_asset_path`, `delete_asset`
- [x] 5.2 `pick_image`: delegate to existing `picker_stage.rs` logic, stage source, generate preview, return `{ jobId, stagedSourcePath, previewPath, previewMimeType }`
- [x] 5.3 `compress_asset`: read staged source, compress via platform processor, write to `<cache>/assets/<assetId>.webp`, compute contentHash, emit `image_pipeline://job_completed`, delete staged source + preview, return `{ jobId }`
- [x] 5.4 `get_asset_path`: check compressed → preview fallback → null, return `{ localPath, contentType }` or `null`
- [x] 5.5 `delete_asset`: idempotent file deletion, succeed silently if file missing

## 6. Simplify lib.rs

- [x] 6.1 Remove `mod job_queue`, `mod pipeline`, `mod queue_state` declarations
- [x] 6.2 Update the invoke handler to register only the 4 commands
- [x] 6.3 Simplify `PluginState` — remove queue-related fields (queue mutex, pipeline paths for jobs.json). Keep: `app: AppHandle<R>`, `cache_root: PathBuf`, processor
- [x] 6.4 Remove all queue recovery, reconciliation, and cleanup logic from `init()`
- [x] 6.5 Keep the `JOB_COMPLETED_EVENT` constant as `"image_pipeline://job_completed"`
- [x] 6.6 Keep the `ImagePipelineExt` trait and `ImagePipeline<R>` public handle
- [x] 6.7 Add TTL-based GC in `init()` — on plugin startup, delete files in `picked/` and `previews/` directories older than 30 minutes since last access. Do NOT touch `assets/` directory (host app manages those via `delete_asset`)

## 7. Update build.rs

- [x] 7.1 Update `COMMANDS` array in `build.rs` to only the 4 command names: `pick_image`, `compress_asset`, `get_asset_path`, `delete_asset`

## 8. Regenerate Permissions

- [x] 8.1 Delete old permission TOMLs in `permissions/autogenerated/commands/` for all 11 old commands
- [x] 8.2 Run `cargo build` to trigger `build.rs` and regenerate permissions for 4 commands
- [x] 8.3 Verify `permissions/autogenerated/commands/` contains exactly: `pick_image.toml`, `compress_asset.toml`, `get_asset_path.toml`, `delete_asset.toml`
- [x] 8.4 Update `permissions/default.toml` to grant all 4 commands by default

## 9. Update guest-js

- [x] 9.1 Rewrite `guest-js/index.ts` — export only: `pickImage`, `compressAsset`, `getAssetPath`, `deleteAsset`
- [x] 9.2 Update TypeScript types to match simplified DTOs (no merchantId, no queue types)
- [x] 9.3 Remove all queue management function exports

## 10. Update Tests

- [x] 10.1 Update `tests/pick_image_contract.rs` — verify `pick_image` returns `{ jobId, stagedSourcePath, previewPath, previewMimeType }` with no merchant_id
- [x] 10.2 Update `tests/dto_error.rs` — remove tests for deleted DTOs, add tests for simplified DTOs (flat cache paths, no merchant_id)
- [ ] 10.3 Add test: `get_asset_path` returns compressed path when file exists
- [ ] 10.4 Add test: `get_asset_path` falls back to preview when no compressed file
- [ ] 10.5 Add test: `get_asset_path` returns null when neither file exists
- [ ] 10.6 Add test: `compress_asset` emits `image_pipeline://job_completed` with correct payload
- [ ] 10.7 Add test: `compress_asset` deletes staged source and preview after success
- [ ] 10.8 Add test: `delete_asset` is idempotent (succeeds on missing file)
- [x] 10.9 Update `tests/android_backend_selection.rs` if needed for simplified interface

## 10B. Update POS App Dialog Dependency

- [x] 10B.1 Update `apps/pos-app/src-tauri/Cargo.toml` line 25: change `tauri-plugin-dialog = "2"` to `tauri-plugin-dialog = { path = "../../../vendor/tauri-plugin-image-pipeline/vendor/tauri-plugin-dialog" }` so both the plugin and the app resolve the same dialog crate instance
- [x] 10B.2 Verify `cargo check --manifest-path apps/pos-app/src-tauri/Cargo.toml` passes — no duplicate crate instances

## 11. Verify Build and Integration

- [x] 11.1 Run `cargo check --manifest-path vendor/tauri-plugin-image-pipeline/Cargo.toml` — verify clean build
- [x] 11.2 Run `cargo test --manifest-path vendor/tauri-plugin-image-pipeline/Cargo.toml` — verify all plugin tests pass
- [x] 11.3 Run `cargo check --manifest-path apps/pos-app/src-tauri/Cargo.toml` — verify POS app builds against simplified plugin
- [x] 11.4 Run `bun test apps/pos-app/src/lib/assets/__test__/` — verify JS tests still pass
- [x] 11.5 Run `bun x ultracite check` — verify no lint errors
- [x] 11.6 Verify POS app capabilities `default.json` permissions match the 4 generated command permissions

## 12. Commit and Push Submodule

- [ ] 12.1 Commit all changes inside `vendor/tauri-plugin-image-pipeline/` to the submodule's own git repo
- [ ] 12.2 Push the submodule commit to `sakti-dev/tauri-plugin-image-pipeline`
- [ ] 12.3 Update sakti-pos to point to the new submodule commit
